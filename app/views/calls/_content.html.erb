<pre>Duration: <%= Time.at(call.duration_in_seconds).utc.strftime("%H:%M:%S") %></pre>
<pre>Status: <%= call.status %></pre>
<% if call.recording_url.present? %>
  <div>
    <%= link_to 'Download recording', call.recording_url, target: '_blank' %>
  </div>
<% end %>
